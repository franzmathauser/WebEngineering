<%= form_for([@song, @song.song_comments.build]) do |f| %>
  

    <%= f.hidden_field :from_duration %>
    <%= f.hidden_field :to_duration %>

  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
